<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Menu</title>
</head>
<body>
  <%- include('./partials/nav') %>
  <h1>Full Menu</h1>
  <u><h2>Mains</h2></u>
  <% RESTAURANT.menu.filter(items => items.category === "mains").forEach( item => { %>
    <h3><%= item.name %>
      <% if(item.rating === 5) { %>
        ⭐️
      <% } %> </h3>
    <h4>Price: $<%= item.price %></h4>
    <h4>Rating: <%= item.rating %> stars</h4>
    <h4>Description: <%= item.details %></h4>

  <% }) %>

  <u><h2>Desserts</h2></u>
  <% RESTAURANT.menu.filter(items => items.category === "desserts").forEach( item => { %>
    <h3><%= item.name %>
      <% if(item.rating === 5) { %>
        ⭐️
      <% } %> </h3>
    <h4>Price: $<%= item.price %></h4>
    <h4>Rating: <%= item.rating %> stars</h4>
    <h4>Description: <%= item.details %></h4>
  
  <% }) %>

  <u><h2>Sides</h2></u>
  <% RESTAURANT.menu.filter(items => items.category === "sides").forEach( item => { %>
    <h3><%= item.name %>
      <% if(item.rating === 5) { %>
        ⭐️
      <% } %> </h3>
    <h4>Price: $<%= item.price %></h4>
    <h4>Rating: <%= item.rating %> stars</h4>
    <h4>Description: <%= item.details %></h4>
  
  <% }) %>

</body>
</html>

